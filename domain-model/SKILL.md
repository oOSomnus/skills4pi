---
name: domain-model
description: 对你的设计方案进行拷问式的盘问，挑战现有领域模型，精炼术语，并在决策明确时即时更新文档（CONTEXT.md、ADR）。当用户想要用项目的语言和已记录的决策来压力测试一个方案时使用。
disable-model-invocation: true
---

对我进行无情的盘问，探究这个方案的每一个方面，直到我们达成共识。沿着设计树的每个分支走，逐个解决决策之间的依赖关系。对每个问题，提供你的推荐答案。

逐个提问，等待每个问题的反馈后再继续。

如果一个问题可以通过探索代码库来回答，就去探索代码库。

## 领域感知

在探索代码库时，还要寻找现有文档：

### 文件结构

大多数仓库只有一个上下文：

```
/
├── CONTEXT.md
├── docs/
│   └── adr/
│       ├── 0001-事件溯源订单.md
│       └── 0002-postgres-写模型.md
└── src/
```

如果根目录存在 `CONTEXT-MAP.md`，则仓库有多个上下文。该映射指向每个上下文的位置：

```
/
├── CONTEXT-MAP.md
├── docs/
│   └── adr/                          ← 系统级决策
├── src/
│   ├── ordering/
│   │   ├── CONTEXT.md
│   │   └── docs/adr/                 ← 上下文特定决策
│   └── billing/
│       ├── CONTEXT.md
│       └── docs/adr/
```

懒加载创建文件——只有当你确实有东西要写时才创建。如果 `CONTEXT.md` 不存在，在第一个术语确定时创建。如果 `docs/adr/` 不存在，在需要第一个 ADR 时创建。

## 会话期间

### 对照词汇表进行挑战

当用户使用的术语与 `CONTEXT.md` 中现有语言冲突时，立即指出来。"你的词汇表将'取消'定义为 X，但你似乎指的是 Y——到底是哪个？"

### 精炼模糊语言

当用户使用模糊或含混的术语时，提出一个精确的规范术语。"你说'账户'——你是指客户还是用户？这是两个不同的概念。"

### 讨论具体场景

当讨论领域关系时，用具体场景来压力测试。编造探索边界情况的场景，迫使用户精确界定概念之间的边界。

### 与代码交叉验证

当用户陈述某事的工作方式时，检查代码是否一致。如果发现矛盾，指出来："你的代码取消了整个订单，但你刚才说部分取消是可能的——哪个是对的？"

### 即时更新 CONTEXT.md

当术语确定后，立即更新 `CONTEXT.md`。不要批量处理——在确定时就记录下来。使用 [CONTEXT-FORMAT.md](./CONTEXT-FORMAT.md) 中的格式。

### 谨慎提供 ADR

只有满足以下三个条件时才提供创建 ADR：

1. **难以逆转**——以后改变主意的成本是有意义的
2. **没有上下文会令人惊讶**——未来的读者会想"他们为什么这样做？"
3. **真正权衡的结果**——确实存在替代方案，你基于具体原因选择了其中之一

如果以上三个条件缺失任何一个，跳过 ADR。使用 [ADR-FORMAT.md](./ADR-FORMAT.md) 中的格式。
